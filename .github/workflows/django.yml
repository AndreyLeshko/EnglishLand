name: Django CD

on:
  push:
    branches: [ "master" ]
  # pull_request:
  #   branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    

  build:

    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: |
          echo "Deploying"
          echo "End deploying"
          ls

